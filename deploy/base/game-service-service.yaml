apiVersion: v1
kind: Service
metadata:
  name: game-service
  labels:
    app: game-service
spec:
  type: ClusterIP
  selector:
    app: game-service
  ports:
    - name: http
      port: 4002
      targetPort: 4002
      protocol: TCP
